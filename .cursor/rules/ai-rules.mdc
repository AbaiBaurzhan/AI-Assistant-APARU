---
alwaysApply: true
------
alwaysApply: true
---
Ты эксперт по созданию FAQ-ассистентов техподдержки на Python, FastAPI и векторных базах данных (FAISS/Qdrant).
Твоя задача — разработать продакшен-готовый сервис, который встроится в существующий чат техподдержки.
Ассистент не генерирует новые тексты, а понимает вопрос пользователя и выбирает готовый ответ из базы знаний (FAQ).

Правила стиля:
- Используй FastAPI + Pydantic v2 для API и схем.
- Все I/O операции реализуй через async def.
- Во всех функциях указывай type hints.
- Используй модульность и функции вместо классов.
- Следуй паттерну RORO (Receive an Object, Return an Object).
- Имена файлов и переменных — snake_case, переменные должны быть говорящими (user_query, confidence_score).
- Логика должна быть чистой и декларативной.

Архитектура:
- Каталоги: routers/, schemas/, utils/, fixtures/.
- Маршруты должны быть декларативными, с чётким response_model.
- В data/faq.xlsx хранится база (колонки: question, answer).
- build_index.py:
  - Читает Excel.
  - Строит эмбеддинги (модель `BAAI/bge-m3`).
  - Сохраняет faiss.index и kb.jsonl.
- В utils/ вынести: нормализацию текста, генерацию эмбеддингов, поиск в индексе, проверку порогов.
- Маршрут /ask:
  - Принимает { "query": "..." }.
  - Конвертирует в эмбеддинг.
  - Делает поиск top-K кандидатов в faiss.index.
  - Возвращает один из вариантов:
    • Если similarity ≥ 0.8 → вернуть готовый answer.
    • Если 0.6 ≤ similarity < 0.8 → вернуть "Уточните, пожалуйста" + список похожих вопросов.
    • Если similarity < 0.6 → вернуть "Не понял вопрос, передаю оператору".
- Маршрут /feedback:
  - Принимает { "query": "...", "answer_id": "...", "feedback": "👍/👎" }.
  - Сохраняет данные в feedback.log или в БД.
- Ошибки — только через HTTPException с понятным описанием.
- Edge cases обрабатывай guard-clauses и early return.

Оптимизация и устойчивость:
- Не использовать блокирующие операции.
- Логировать все ошибки и вызовы функций.
- Ответы API должны быть короткими и структурированными:
  { "reply": "...", "confidence": 0.85, "source": "q001" }
- Для часто используемых данных применяй кэш.

Выведи весь код проекта:
- build_index.py
- app.py
- routers/ask.py
- schemas/ask.py
- utils/search.py
- requirements.txt
- пример структуры faq.xlsx (2–3 строки).
